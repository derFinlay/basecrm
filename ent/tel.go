// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/derfinlay/basecrm/ent/customer"
	"github.com/derfinlay/basecrm/ent/tel"
)

// Tel is the model entity for the Tel schema.
type Tel struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// Tel holds the value of the "tel" field.
	Tel string `json:"tel,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the TelQuery when eager-loading is set.
	Edges         TelEdges `json:"edges"`
	customer_tels *int
	selectValues  sql.SelectValues
}

// TelEdges holds the relations/edges for other nodes in the graph.
type TelEdges struct {
	// Notes holds the value of the notes edge.
	Notes []*Note `json:"notes,omitempty"`
	// Customer holds the value of the customer edge.
	Customer *Customer `json:"customer,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
}

// NotesOrErr returns the Notes value or an error if the edge
// was not loaded in eager-loading.
func (e TelEdges) NotesOrErr() ([]*Note, error) {
	if e.loadedTypes[0] {
		return e.Notes, nil
	}
	return nil, &NotLoadedError{edge: "notes"}
}

// CustomerOrErr returns the Customer value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e TelEdges) CustomerOrErr() (*Customer, error) {
	if e.loadedTypes[1] {
		if e.Customer == nil {
			// Edge was loaded but was not found.
			return nil, &NotFoundError{label: customer.Label}
		}
		return e.Customer, nil
	}
	return nil, &NotLoadedError{edge: "customer"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*Tel) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case tel.FieldID:
			values[i] = new(sql.NullInt64)
		case tel.FieldTel:
			values[i] = new(sql.NullString)
		case tel.FieldCreatedAt, tel.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		case tel.ForeignKeys[0]: // customer_tels
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the Tel fields.
func (t *Tel) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case tel.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			t.ID = int(value.Int64)
		case tel.FieldTel:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field tel", values[i])
			} else if value.Valid {
				t.Tel = value.String
			}
		case tel.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				t.CreatedAt = value.Time
			}
		case tel.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				t.UpdatedAt = value.Time
			}
		case tel.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field customer_tels", value)
			} else if value.Valid {
				t.customer_tels = new(int)
				*t.customer_tels = int(value.Int64)
			}
		default:
			t.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the Tel.
// This includes values selected through modifiers, order, etc.
func (t *Tel) Value(name string) (ent.Value, error) {
	return t.selectValues.Get(name)
}

// QueryNotes queries the "notes" edge of the Tel entity.
func (t *Tel) QueryNotes() *NoteQuery {
	return NewTelClient(t.config).QueryNotes(t)
}

// QueryCustomer queries the "customer" edge of the Tel entity.
func (t *Tel) QueryCustomer() *CustomerQuery {
	return NewTelClient(t.config).QueryCustomer(t)
}

// Update returns a builder for updating this Tel.
// Note that you need to call Tel.Unwrap() before calling this method if this Tel
// was returned from a transaction, and the transaction was committed or rolled back.
func (t *Tel) Update() *TelUpdateOne {
	return NewTelClient(t.config).UpdateOne(t)
}

// Unwrap unwraps the Tel entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (t *Tel) Unwrap() *Tel {
	_tx, ok := t.config.driver.(*txDriver)
	if !ok {
		panic("ent: Tel is not a transactional entity")
	}
	t.config.driver = _tx.drv
	return t
}

// String implements the fmt.Stringer.
func (t *Tel) String() string {
	var builder strings.Builder
	builder.WriteString("Tel(")
	builder.WriteString(fmt.Sprintf("id=%v, ", t.ID))
	builder.WriteString("tel=")
	builder.WriteString(t.Tel)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(t.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(t.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// Tels is a parsable slice of Tel.
type Tels []*Tel
